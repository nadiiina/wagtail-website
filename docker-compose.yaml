version: '3.8'

services:
  wagtail:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      PORT: 8000
      HOST_NAME: "http://localhost:8000"
      CSRF_TRUSTED_ORIGINS: "http://localhost:8000"
      DATABASE_URL: "postgres://postgres:admin123d@172.31.48.156:5432/postgres"
      DJANGO_ALLOWED_HOSTS: "*"
